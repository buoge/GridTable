# Grid Layout 组件功能规格说明

## 1. 文档目的

本文用于回顾 `GridLayout.vue` 现有组件整体能力，说明它支持哪些展示状态、数据结构、模板编辑交互、表单填报/只读展示能力，以及各类操作对数据的影响。



## 2. 组件定位

GridLayout 是一个二维表格式布局容器。它不是普通静态 table，而是一个可以承载表单子组件的布局组件。

它的核心职责包括：

- 以行列结构展示表单子组件
- 在模板编辑态支持子组件拖入单元格
- 在模板编辑态支持行、列、单元格相关配置
- 在表单填报态渲染可填写的子组件
- 在只读显示态渲染子组件的已填写值
- 基于同一份 `config.cells` 数据稳定回显布局和内容

## 3. 支持的页面状态

### 3.1 模板编辑态

由 `edit = true` 控制。

模板编辑态支持：

- 选中 Grid Layout 整体组件
- 选中行
- 选中列
- 选中单元格
- 连续选择单元格
- 拖拽子组件到空单元格
- 行新增、删除、复制
- 列新增、删除、复制
- 列宽拖拽调整
- 单元格背景色设置
- 单元格合并、取消合并
- 清空单元格内容

### 3.2 表单填报态

填报态支持：

- 渲染 Grid Layout 标题
- 渲染表头
- 按已保存的行列、合并、背景色展示表格
- 在单元格中渲染可填写的子组件
- 子组件值仍由子组件自身负责提交

### 3.3 只读显示态

业务只读模式进入。

只读显示态支持：

- 渲染 Grid Layout 标题
- 渲染表头
- 按已保存布局展示表格
- 在单元格中展示子组件只读值
- 不提供模板编辑交互

## 4. 核心数据模型

Grid Table 父组件的核心数据保存在 `item.config` 中。

```json
{
  "label": "Grid Layout",
  "rowNum": 3,
  "columnWidths": [400, 400, 400],
  "headerLabels": ["Column 1", "Column 2", "Column 3"],
  "cells": {
    "0:0": {
      "fieldIds": [1226726]
    },
    "1:0": {
      "fieldIds": [1226729],
      "colSpan": 2,
      "backgroundColor": "#e5f2ff"
    }
  }
}
```

### 4.1 顶层字段

| 字段             | 含义           |
| -------------- | ------------ |
| `label`        | 表格标题         |
| `rowNum`       | 表格行数         |
| `columnWidths` | 列宽数组，数组长度即列数 |
| `headerLabels` | 表头文案         |
| `cells`        | 单元格配置集合      |

### 4.2 Cell 字段

| 字段                | 含义                    |
| ----------------- | --------------------- |
| `fieldIds`        | 当前单元格内子组件 field id 列表 |
| `fieldUuids`      | 新增或复制过程中的临时子组件 uuid   |
| `colSpan`         | 当前单元格横向占用列数，默认 `1`    |
| `rowSpan`         | 当前单元格纵向占用行数，默认 `1`    |
| `backgroundColor` | 单元格背景色                |

### 4.3 子组件关系

Grid Table 子组件仍然是独立 field。父组件通过 `cells[fieldIds]` 表达子组件归属关系。

```json
{
  "cells": {
    "0:0": {
      "fieldIds": [1226726]
    }
  }
}
```

表示 id 为 `1226726` 的子组件位于第一行第一列。

## 5. 渲染流程



### 5.1 行列生成

列数来自 `columnWidths.length`。行数来自 `rowNum`。

运行态会派生：

- `localColumnWidths`
- `localRowHeights`
- `localRows`

当前行高不持久化具体高度，而是用默认最小高度生成：

```css
grid-template-rows: minmax(44px, auto);
```

`44px` 来自 `DEFAULT_ROW_HEIGHT`，用于保证空行也有基础高度；内容较多时行高可以自动撑开。

### 5.2 子组件挂载

`slotChildrenMap` 根据 `cells.fieldIds` 找到真实子组件：

```js
cells["0:0"].fieldIds = [1226726];
slotChildrenMap[0] = [field1226726];
```

如果存在 `fieldUuids`，运行态也会尝试用 uuid 匹配临时新增/复制出来的子组件。

### 5.3 合并格处理

当前持久化结构中没有单独的 `cellMerges` 字段。合并信息直接保存在 `cells` 的单元格对象上：

```json
{
  "1:0": {
    "colSpan": 2,
    "rowSpan": 1
  }
}
```

运行态会从 `cells.colSpan`、`cells.rowSpan` 派生出合并元数据，供 `buildCoveredCells` 和 `buildGridCells` 使用。

`buildCoveredCells` 根据合并信息计算被覆盖的格子。被覆盖的格子不单独渲染 DOM，由起始格通过 CSS Grid span 占位。

示例：

```json
{
  "1:0": {
    "colSpan": 2
  }
}
```

会让 `"1:0"` 横向占 2 列，`"1:1"` 作为 covered cell 被跳过。

### 

## 6. 模板编辑态功能

### 6.1 整体组件选中

点击 Grid Layout 外层或标题时，调用 `selectComponent()`：

- 选中 Grid Layout 父组件
- 清空行、列、单元格选中态
- 关闭行列菜单和单元格菜单

### 6.2 行选择与行菜单

编辑态左侧有行操作手柄。点击行手柄会：

- 选中该行
- 显示行菜单
- 清空列选中和单元格选中

行菜单能力：

| 菜单项          | 功能          |
| ------------ | ----------- |
| Insert above | 在当前行上方插入新行  |
| Insert below | 在当前行下方插入新行  |
| Duplicate    | 复制当前行及行内子组件 |
| Delete       | 删除当前行及行内子组件 |

删除行时，如果只剩 1 行，则不允许继续删除。

### 6.3 列选择与列菜单

编辑态表头中有列操作按钮。点击列按钮会：

- 选中该列
- 显示列菜单
- 清空行选中和单元格选中

列菜单能力：

| 菜单项          | 功能          |
| ------------ | ----------- |
| Insert left  | 在当前列左侧插入新列  |
| Insert right | 在当前列右侧插入新列  |
| Duplicate    | 复制当前列及列内子组件 |
| Delete       | 删除当前列及列内子组件 |

删除列时，如果只剩 1 列，则不允许继续删除。

### 6.4 列宽调整

编辑态表头列之间存在拖拽手柄。

交互行为：

- 按下表头 resizer 开始拖拽
- 鼠标移动时实时更新本地列宽
- 鼠标松开后持久化 `columnWidths`
- 单列最小宽度为 `MIN_COLUMN_WIDTH = 120`

列宽调整会影响所有行，因为当前表格列宽是整表统一的。

### 6.5 单元格选择

空单元格点击后会选中该单元格。

有子组件的单元格：

- 普通点击子组件区域：选中子组件
- `Cmd` / `Ctrl` / `Shift` 点击：进入单元格选择逻辑

连续选择规则：

- 已有单元格选中时，按 `Cmd` / `Ctrl` / `Shift` 点击另一个单元格，会选择两个点之间的连续区间
- 如果先普通点击选中了某个子组件，再按 `Cmd` / `Ctrl` 点击另一个单元格，会以该子组件所在单元格为锚点做连续选择
- 仅支持同一行或同一列的连续选择
- 被合并覆盖的隐藏格不会进入选区

### 6.6 单元格菜单

选中单元格后，右侧会出现单元格操作手柄。点击手柄打开单元格菜单。

单元格菜单能力：

| 菜单项            | 功能          |
| -------------- | ----------- |
| Color          | 设置选中单元格背景色  |
| Merge cells    | 合并连续选择的单元格  |
| Unmerge cells  | 取消当前合并单元格   |
| Clear contents | 清空选中单元格内子组件 |

### 6.7 背景色设置

支持的背景色来自 `CELL_BACKGROUND_COLORS`：

| 名称                 | 值         |
| ------------------ | --------- |
| Default background | 空字符串      |
| Gray background    | `#f0efec` |
| Brown background   | `#f7ece6` |
| Orange background  | `#fdebdc` |
| Yellow background  | `#fff6d8` |
| Green background   | `#e8f3ec` |
| Blue background    | `#e5f2ff` |
| Purple background  | `#f0e8fb` |
| Pink background    | `#fbecf2` |

设置背景色会写入对应 cell 的 `backgroundColor`。

选择 Default background 会删除 `backgroundColor` 字段。

### 6.8 合并单元格

合并条件：

- 至少选中 2 个单元格
- 单元格必须在同一行或同一列
- 选区必须连续
- 选区不能包含被合并覆盖的隐藏格

合并行为：

- 起始单元格写入 `colSpan` 或 `rowSpan`
- 被覆盖单元格从 `cells` 中删除
- 被覆盖单元格内的子组件会被删除
- 起始单元格保留原背景色

### 6.9 取消合并

取消合并会删除起始 cell 上的 `colSpan` 和 `rowSpan`。

取消合并后：

- 原起始格保留已有内容和背景色
- 被展开出来的其他格子为空格

### 6.10 清空单元格内容

Clear contents 会删除选中单元格中的子组件。

如果选中的是合并起始格，会同时处理该合并区域覆盖范围内的内容。

清空内容会：

- 删除相关子组件 field
- 从 layout 中移除对应子节点
- 从 `cells.fieldIds` 中移除对应 id
- 保留单元格本身的合并和背景色配置

### 6.11 拖拽添加子组件

编辑态空单元格支持拖拽添加子组件。



拖入行为：

- 只能拖入空单元格
- 每个单元格当前只允许放入一个组件
- 放入后通过 `componentAddComponent` 新增子组件
- 新增成功后父组件 `cells` 会回填子组件 id

如果目标单元格已占用，会提示：

```text
Each space can only contain one component
```

## 7. 行列数据操作行为

### 7.1 插入行

插入行会：

- `rowNum` 增加
- 插入点之后的 `cells` key 后移
- 新行为空行
- 插入后选中新行

### 7.2 删除行

删除行会：

- 删除该行内子组件
- 删除该行对应 cell 或调整被影响的合并范围
- 删除行之后的 `cells` key 前移
- 更新父组件配置和 layout

### 7.3 复制行

复制行会：

- 在当前行下方新增一行
- 复制该行列结构
- 复制该行内子组件
- 新复制子组件先通过 `fieldUuids` 挂到目标 cell
- 服务端生成真实 id 后回填为 `fieldIds`
- 回填完成后删除复制子组件上的临时 `row`、`col`、`slotIndex`

### 7.4 插入列

插入列会：

- 在指定位置新增列宽
- 新增表头 label
- 插入点之后的 `cells` key 后移
- 新列为空列
- 插入后选中新列

### 7.5 删除列

删除列会：

- 删除该列内子组件
- 删除该列对应 cell 或调整被影响的合并范围
- 删除列之后的 `cells` key 前移
- 删除对应表头和列宽

### 7.6 复制列

复制列会：

- 在当前列右侧新增列
- 复制当前列表头和列宽
- 复制当前列内子组件
- 新复制子组件通过 `fieldUuids` 临时挂载
- 服务端生成真实 id 后回填为 `fieldIds`

## 8. 填报态与只读态功能

### 8.1 填报态

填报态不展示模板编辑菜单、行列手柄、单元格操作手柄。

填报态支持：

- 展示表格标题
- 展示表头
- 按 `columnWidths`、`rowNum`、`cells` 渲染二维结构
- 按 `cells.fieldIds` 渲染子组件
- 子组件保持自己的填写能力
- 空单元格展示为空白区域

### 8.2 只读态

只读态不展示模板编辑菜单、行列手柄、单元格操作手柄。

只读态支持：

- 展示表格标题
- 展示表头
- 按保存后的布局展示二维表格
- 按 `cells.fieldIds` 渲染子组件只读值
- 空单元格展示为空白区域

## 9. 状态同步与互斥规则

Grid Table 内部有多类选中状态：

- 父组件选中
- 行选中
- 列选中
- 单元格选中
- 子组件选中

互斥规则：

- 选中父组件会清空行、列、单元格选中
- 选中行会清空列和单元格选中
- 选中列会清空行和单元格选中
- 选中子组件会清空单元格选中
- 点击组件外部会清空当前 Grid Table 的行、列、单元格 active 状态

这些规则用于避免多个操作上下文同时激活，导致菜单、手柄、蓝色边框状态错乱。

## 10. 持久化行为

Grid Layout 的保存统一通过父组件 config 更新。

主要写入字段：

- `columnWidths`
- `rowNum`
- `headerLabels`
- `cells`

持久化前会清理历史兼容字段，避免旧结构继续进入最终保存结果：

- `rows`
- `cellMeta`
- `mergedCells`
- `coveredCells`
- `rowHeights`
- `minHeight`
- `cellStyles`
- `cellMerges`

最终保存结构以 `cells` 为准。

## 11. 当前边界与约束

当前实现存在以下边界：

- 每个单元格当前只允许拖入一个组件
- 可拖入组件类型限制为 `1` 到 `12`
- 单元格连续选择只支持同一行或同一列
- 合并只支持横向连续或纵向连续，不支持矩形区域合并
- 行高不作为最终持久化结构保存，运行态以默认最小高度和内容自适应为主
- 移动端只消费已保存结构，不提供模板编辑能力

## 12. 快速功能清单

| 功能        | 模板编辑态 | 填报态 | 只读态 |
| --------- | ----- | --- | --- |
| 展示标题      | 支持    | 支持  | 支持  |
| 展示表头      | 支持    | 支持  | 支持  |
| 展示子组件     | 支持    | 支持  | 支持  |
| 填写子组件值    | 不适用   | 支持  | 不支持 |
| 子组件只读展示   | 不适用   | 不适用 | 支持  |
| 拖拽添加子组件   | 支持    | 不支持 | 不支持 |
| 行新增/删除/复制 | 支持    | 不支持 | 不支持 |
| 列新增/删除/复制 | 支持    | 不支持 | 不支持 |
| 列宽调整      | 支持    | 不支持 | 不支持 |
| 单元格选择     | 支持    | 不支持 | 不支持 |
| 连续选择      | 支持    | 不支持 | 不支持 |
| 背景色设置     | 支持    | 不支持 | 不支持 |
| 合并/取消合并   | 支持    | 不支持 | 不支持 |
| 清空单元格内容   | 支持    | 不支持 | 不支持 |
